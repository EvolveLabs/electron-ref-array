{
  "name": "electron-ref-array",
  "description": "This is a fork of the ref-array project, adding electron binary support",
  "keywords": [
    "array",
    "ref"
  ],
  "version": "1.1.3",
  "author": "Justin Chase <justin@evolvelabs.com>",
  "repository": {
    "type": "git",
    "url": "git://github.com/evolvelabs/electron-ref-array.git"
  },
  "main": "./lib/array.js",
  "scripts": {
    "test": "pushd test && bash ../node_modules/.bin/elb configure && popd && mocha -gc --reporter spec"
  },
  "engines": {
    "node": ">=2.2.1"
  },
  "dependencies": {
    "debug": "2",
    "array-index": "~0.1.0",
    "electron-ref": "~1.0.9"
  },
  "devDependencies": {
    "bindings": "~1.2.0",
    "mocha": "*",
    "nan": "~1.8.4",
    "electron-updater-tools": "~0.1.x"
  }
}
